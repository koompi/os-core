# Description: low-level libraries useful for providing data structure handling for C, portability wrappers and interfaces for such runtime functionality as an event loop, threads, dynamic loading and an object system.
# URL: http://www.linuxfromscratch.org/blfs/view/systemd/general/glib2.html
# Maintainer: Jiren Edison
# Organization: KOOMPI
# Depends on: libxslt, pcre

name=glib
version=
release=1
source=(https://download.gnome.org/sources/glib/2.66/glib-2.66.4.tar.xz
	http://www.linuxfromscratch.org/patches/blfs/svn/glib-2.66.4-skip_warnings-1.patch)

build() {
	cd $name-$version

	patch -Np1 -i ../glib-2.66.4-skip_warnings-1.patch

mkdir build &&
cd    build &&

meson --prefix=/usr      \
      -Dman=true         \
      -Dselinux=disabled \
      ..                 &&
ninja
DESTDIR=$PKG ninja install


mkdir -p /usr/share/doc/glib-2.66.4 &&
cp -r ../docs/reference/{NEWS,gio,glib,gobject} /usr/share/doc/glib-2.66.4

rm -f /usr/include/glib-2.0/glib/gurifuncs.h


}
