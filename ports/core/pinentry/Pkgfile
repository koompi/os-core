Description: contains a collection of simple PIN or pass-phrase entry dialogs which utilize the Assuan protocol as described by the Ägypten project. 
# URL: https://koompi.org        
# Maintainer:  HangsiaHONG, hangsia@koompi.org
# Organization:  KOOMPI, koompi.com
# Depends on:  libassuan libgpg-error

name=pinentry 
version=1.1.0
release=1
source=(https://www.gnupg.org/ftp/gcrypt/pinentry/pinentry-1.1.0.tar.bz2)

build() {
        cd $name-$version

        ./configure --prefix=/usr --enable-pinentry-tty         make
        make
        make DESTDIR=$PKG install
}
