# Description: The libgpg-error package contains a library that defines common error values for all GnuPG components.
# URL: https://koompi.org        
# Maintainer:  HangsiaHONG, hangsia@koompi.org
# Organization:  KOOMPI, koompi.com
# Depends on:  libunistring

name=libidn2
version=2.3.0
release=1
source=(https://www.gnupg.org/ftp/gcrypt/libgpg-error/libgpg-error-1.39.tar.bz2)

build() {
        cd $name-$version
	./configure --prefix=/usr --disable-static &&
	make
        make DESTDIR=$PKG install
}
