[x]1. Check the requirement for install KFS with `check_requirement` script.
[x]2. Download what dependencies that are missing.
[x]3. Run the automate script for install deps: {
  note: This script should be run on VirtualBox.
  Recommend host: ubuntu or arch linux.
  if ubuntu:
    run `install_deps`
  if arch:
    run `install_deps`
    _ "The script will download the missing dependencies for building KFS"
}

[x]4. Create config file for setup configuration for building toolchains.
[x]5. Create 01-build-toolchains script
[]6. Create 02-base______ later..:













